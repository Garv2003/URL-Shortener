<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <link rel="stylesheet" href="../styles/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js">
    </script>
</head>

<body class="bg-dark bg-gradient d-flex flex-column min-vh-100">
    <h1 class="container-fluid text-center bg-dark text-primary p-3 shadow-lg">
        URl Shortener
    </h1>
    <div class="container-sm bg-dark text-light">
        <div class="btn">
            <button class="url slider">URL</button>
            <button class="qrcode ">QR CODE</button>
        </div>
        <div id="cont1">
            <div class="form_url">
                <a href="/"></a>
                <input type="text" class="forminput" id="copyinput" value=http://localhost:1234/{{url.ShortUrl}}>
                <button class="formbtn" id="btn">Copy URL</button>
            </div>
            <div class="box">
                <div class="fullurl">
                    <div>Full URL</div>
                    <div class="ellipsis">
                        <span>
                            <a href="{{url.FullUrl}}">
                                {{url.FullUrl}}
                            </a>
                        </span>
                    </div>
                </div>
                <div class="clicks">
                    <div>Total of clicks of Your shortened URL</div>
                    <span>{{url.Clicks}}</span>
                </div>
                <button class="returnbtn">
                    <a href="/">Shorten another URL</a>
                </button>
            </div>
        </div>

        <div class="container2 hidden" id="cont2">
            <img src="https://chart.googleapis.com/chart?cht=qr&chl=Hello+World&chs=160x160&chld=L|0" class="qr-code"
                alt="">
            <div class="box">
                <div class="fullurl">
                    <div>Full URL</div>
                    <div class="ellipsis">
                        <span>
                            <a href="{{url.FullUrl}}">
                                {{url.FullUrl}}
                            </a>
                        </span>
                    </div>
                </div>
                <div class="clicks">
                    <div>Total of clicks of Your shortened URL</div>
                    <span>{{url.Clicks}}</span>
                </div>
                <button class="returnbtn">
                    <a href="/">Shorten another URL</a>
                </button>
            </div>
        </div>
    </div>
    <div class="container-fluid text-light text-center border-top border-primary border-5  mt-auto">
        <div class="fs-3 p-2">
            © 2023 URL SHORTENER - Tool to shorten a long link
        </div>
        <div class="d-flex justify-content-center fs-5 p-4 ">
            <div class="fw-bolder mx-2 Size">ShortURL</div>
            <div class="fw-bolder mx-2 Size">URL Click Counter</div>
            <div class="fw-bolder mx-2 Size">Unshorten URL </div>
            <div class="fw-bolder mx-2 Size">Report Malicious URL</div>
            <div class="fw-bolder mx-2 Size">Terms of Service </div>
            <div class="fw-bolder mx-2 Size">Privacy </div>
            <div class="fw-bolder mx-2 Size">Contact</div>
        </div>
    </div>
    <script src="../js/script.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>